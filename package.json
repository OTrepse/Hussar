{
  "name": "hussar",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend for a Polish pronunciation accuracy app.",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "typecheck": "npm run typecheck --workspace frontend"
  }
}
